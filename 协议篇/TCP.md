### TCP简介
> TCP是传输层的协议，是一种稳定，可靠，高效的一种传输协议。全双工协议

> TCP有6种标识 SYN(建立连接) ACK(确认) PSH(传送) FIN(结束) RST(重置) URG(紧急)

#### TCP三次握手
> 第一次客户端主动握手 client 发送标志位为SYN=1 Seq=0 请求 server 连接
> 第二次服务端收到通知后 server 发送标志位为ACK=1 SYN=1 给 client 端
> 第三次客户端收到后 发送 报文 ACK=1 Seq=1 链接成功，开始发送数据

#### 为什么是三次握手，不是两次或者四次？
> 客户端请求建立链接可能由于一些原因导致，服务端收到请求后，客户端已经不在线，已经失效。但是服务端还是要继续给出确认，这里需要客户端再次确认，
发送一个 ACK报文，表示我已经做好发送数据的准备了。综上所述，三次握手最合适。
[TCP详解](https://www.cnblogs.com/qdhxhz/p/8470997.html)

#### 四次挥手
> 客户端发送 FIN 关闭链接，并且停止发送数据，服务端收到 FIN 返回一个 ACK 。服务端继续返回一个 FIN和ACK。客户端收到后发送一个 ACK
连接关闭



























