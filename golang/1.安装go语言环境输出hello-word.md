### 安装 go 语言并 输出 hello word

#### 建议阅读书籍
<a href="https://books.studygolang.com/gopl-zh/ch0/ch0-02.html" target="_blank">《go语言圣经》</a>

#### 特点
* 开源
* 被称为21实际的c语言
* 特别适合编写网络服务相关的基础设施
* 高性能

#### 安装go语言运行环境

* <a href="https://golang.google.cn/dl/">Downloads</a>进入网站根据自己系统下载程序
* 我是安装的 windows 版本，所以我加入一下环境变量
* go version 会输出当前安装的版本。

#### 输出第一个 hello word
```go
//表示这个文件属于那个包
package main
//导入包
import "fmt"

func main() {
    //包名 . 函数名。Println 是 fmt包中的一个函数
    fmt.Println("hello word!")
}	
```
* $ go run test.go 执行 go命令
* $ hello word! 输出 hello word!

> go语言是一门编译型语言，Go语言的工具链将源代码及其依赖转换成计算机的机器指令（译注：静态编译)
Go语言提供的工具都通过一个单独的命令go调用,go命令有一系列子命令。最简单的一个子命令就是run。这个命令编译一个或多个以.go结尾的源文件，链接库文件，并运行最终生成的可执行文件。

* go build test.go  build 子命令编译称二进制文件，预先编译。
* ./test.go  然后执行这个文件。和 go run test.go 不同的是，一个是直接执行，一个是预先编译，不执行

#### 注意
* go语言不像其他语言需要在语句末尾加分号
* 函数的 { 左括号必须要和 func 在同一行

> 这就是我第一次看手册入门go语言得到的基础知识
